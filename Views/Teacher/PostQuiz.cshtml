@{
    ViewBag.Title = "PostQuiz";
}
@using MvcApplication3.Models
<div class="row">
    <div class="col-sm-2">
    </div>
    <div class="col-sm-6">
        <h1>Selection of Quiz you have created.</h1>
    </div>
    </div>
<br /><br />
    <div class="container">
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                <div class="login-panel panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title">Select Quiz</h3>
                    </div>
                    <div class="panel-body">
                        <form id="Announce" role="form" action="PostQuiz1" method="get" enctype="multipart/form-data">
                            <fieldset>
                                <select class="form-control" name="type" form="Announce">
                                    @foreach (var s in Model)
                                    {

                                        <option>@s</option>

                                    }
                                </select>
                                <!-- Change this to a button or input when using this as a form -->
                                <button type="submit" class="btn btn-lg btn-success btn-block">Select</button>
                            </fieldset>
                        </form>

                    </div>
                </div>
            </div>
        </div>
    </div>
<br /><br />
